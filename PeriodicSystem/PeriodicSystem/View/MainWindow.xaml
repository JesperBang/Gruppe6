<Window x:Class="PeriodicSystem.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PeriodicSystem"
        xmlns:ViewModel="clr-namespace:PeriodicSystem.ViewModel"
        xmlns:View="clr-namespace:PeriodicSystem.View"
        mc:Ignorable="d"
        Title="pTable" Height="720" Width="1280">

    <Window.DataContext>
        <ViewModel:ViewModel />
    </Window.DataContext>


    <StackPanel Orientation="Horizontal">
        <View:SidePanelUserControl/>

        <Grid>
            <ItemsControl ItemsSource="{Binding Atoms}" />
            <ItemsControl ItemsSource="{Binding Binding}" />

            <!--
        <Canvas HorizontalAlignment="Left" Height="652" Margin="569,10,0,0" VerticalAlignment="Top" Width="644"  />
        <ScrollBar x:Name="scrollBar" HorizontalAlignment="Left" Margin="1265,8,0,0" VerticalAlignment="Top" Height="636" Width="15" RenderTransformOrigin="0.5,0.5">
            <ScrollBar.RenderTransform>
                <TransformGroup>
                    <ScaleTransform ScaleX="-1"/>
                    <SkewTransform/>
                    <RotateTransform/>
                    <TranslateTransform/>
                </TransformGroup>
            </ScrollBar.RenderTransform>
        </ScrollBar>
        -->
        </Grid>
    </StackPanel>
    
</Window>
